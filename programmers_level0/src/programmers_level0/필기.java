package programmers_level0;

public class 필기 {
	public static void main(String[] arg) {
		// 포함된 문자 확인
		// (str1.contains(str2));
		// 제곱근 확인

		// Math.sqrt(n)
		// replaceAll("[str]","");

		// char값이 int형인지
		// Character.isDigit(my_string.charAt(i))

		// 반복
		// str.repeat('개수')

		// 리스트 오름차순 정렬
		//Collections.sort(arr);
		
		// 제곱근 구하기
		// int num = (int) Math.sqrt(9); -> 3
		
		// arr에 java가 포함되어있는지 확인
		//  arr.contains("java")))
		
		// 배열 a = 배열 b 이렇게 할경우 값이 같이 변경 됨으로 깊은 배열 복사를 해야함
		// int[] a = b.clone()
		
		// 정수의 2진수 1의 개수를 구함
		//  Integer.bitCount(compare)
		
		// 정수를 2진수로 변환
		// Integer.toBinaryString(n);

		
	}
}
